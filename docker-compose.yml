version: '3.7'
services:

  greeting-service:
    image: kmmanu/greeting-service:0.1.0-SNAPSHOT
    ports:
      - "8081:8081"
    restart: always
    networks:
      - spring-boot-test
      
# Networks to be created to facilitate communication between containers
networks:
  spring-boot-test: